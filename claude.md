# Claude Code - Критерии качества для проекта Lighthouses

Этот документ определяет стандарты качества и процессы разработки для проекта Lighthouses.

## 📋 Основные принципы

### 1. Test-Driven Development (TDD)

**Обязательно:**
- ❌ **НЕ ПИШЕМ КОД БЕЗ ТЕСТОВ**
- ✅ Сначала пишем тест → потом реализацию
- ✅ Каждая фича должна иметь автотесты
- ✅ Минимальное покрытие тестами: 80%

**Типы тестов:**
1. **Unit tests** - тесты функций и утилит
2. **Component tests** - тесты React компонентов
3. **Integration tests** - тесты API endpoints
4. **E2E tests** (опционально) - сквозные тесты критических сценариев

**Инструменты:**
- Jest - unit и integration тесты
- React Testing Library - тесты компонентов
- Playwright/Cypress - E2E тесты (будущее)

### 2. Соответствие PRD (Product Requirements Document)

**Правила:**
- ✅ Каждая фича должна соответствовать требованиям из `prd.md`
- ✅ Перед началом работы - сверяемся с PRD
- ✅ При изменении требований - ОБЯЗАТЕЛЬНО обновляем `prd.md`
- ✅ Документируем отклонения от PRD с обоснованием

**Процесс изменения PRD:**
1. Обсуждение с пользователем
2. Обновление `prd.md`
3. Отметка в `BACKLOG.md`
4. Commit с изменениями PRD

### 3. Управление задачами

**Файлы:**
- `BACKLOG.md` - беклог задач, приоритеты, прогресс
- `prd.md` - продуктовые требования
- `claude.md` - этот файл с критериями качества

**BACKLOG.md содержит:**
- Текущие задачи (In Progress)
- Очередь задач (To Do)
- Выполненные задачи (Done)
- Будущие планы (Future)
- Приоритеты (P0-P2)

## 🔧 Процесс разработки

### Цикл разработки фичи:

```
1. Проверка PRD
   └─> Соответствует ли требованиям?

2. Написание тестов (TDD)
   └─> Тесты ДОЛЖНЫ падать (red)

3. Реализация
   └─> Минимальная реализация для прохождения тестов (green)

4. Рефакторинг
   └─> Улучшение кода без изменения поведения (refactor)

5. Проверка
   └─> Все тесты проходят?
   └─> Соответствует PRD?
   └─> Обновлен BACKLOG?

6. Commit
   └─> Понятное сообщение с контекстом
```

### Перед каждым commit:

```bash
# 1. Запустить все тесты
npm test

# 2. Проверить типы TypeScript
npm run type-check

# 3. Проверить линтер
npm run lint

# 4. Проверить форматирование
npm run format:check
```

## ✅ Definition of Done (DoD)

Фича считается готовой когда:

- [ ] ✅ Написаны автотесты (unit + integration)
- [ ] ✅ Все тесты проходят (`npm test`)
- [ ] ✅ Соответствует требованиям из `prd.md`
- [ ] ✅ Нет TypeScript ошибок (`npm run type-check`)
- [ ] ✅ Нет ошибок линтера (`npm run lint`)
- [ ] ✅ Код отформатирован (`npm run format`)
- [ ] ✅ Обновлен `BACKLOG.md`
- [ ] ✅ Создан осмысленный commit
- [ ] ✅ Локально проверено вручную

## 🚫 Что делать НЕЛЬЗЯ

1. ❌ Коммитить код без тестов
2. ❌ Пушить код с падающими тестами
3. ❌ Игнорировать TypeScript ошибки
4. ❌ Отклоняться от PRD без документирования
5. ❌ Забывать обновлять BACKLOG.md
6. ❌ Коммитить generated код (node_modules, .next, etc)
7. ❌ Хардкодить конфигурацию (используем .env)

## 🎯 Примеры

### ✅ ХОРОШО:

```typescript
// 1. Сначала тест (goals.test.ts)
describe('POST /api/goals', () => {
  it('should create a new goal with valid data', async () => {
    const response = await fetch('/api/goals', {
      method: 'POST',
      body: JSON.stringify({ title: 'Test Goal' })
    });
    expect(response.status).toBe(201);
    const goal = await response.json();
    expect(goal.title).toBe('Test Goal');
  });
});

// 2. Потом реализация (route.ts)
export async function POST(request: NextRequest) {
  const { title } = await request.json();
  const goal = await prisma.goal.create({ data: { title } });
  return NextResponse.json(goal, { status: 201 });
}
```

### ❌ ПЛОХО:

```typescript
// Сначала написали код, потом забыли про тесты
export async function POST(request: NextRequest) {
  const { title } = await request.json();
  const goal = await prisma.goal.create({ data: { title } });
  return NextResponse.json(goal, { status: 201 });
}
// ❌ Нет тестов!
```

## 📊 Метрики качества

Отслеживаем:
- **Test Coverage**: минимум 80%
- **TypeScript Strict**: включен
- **Build Success Rate**: 100% перед push
- **PRD Compliance**: 100% фич соответствуют требованиям

## 🔄 CI/CD Pipeline (планируется)

```yaml
on: [push, pull_request]
jobs:
  test:
    - Install dependencies
    - Generate Prisma Client
    - Run linter
    - Run type check
    - Run tests
    - Check coverage (>80%)
    - Build project
```

## 📝 Commit Messages

Формат:
```
<type>: <short description>

<detailed description>
<reference to BACKLOG/PRD if applicable>

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
```

Types: `feat`, `fix`, `test`, `refactor`, `docs`, `chore`

## 🆘 Если что-то сломалось

1. Откатить последний commit
2. Написать тест, воспроизводящий проблему
3. Исправить код
4. Убедиться что все тесты проходят
5. Закоммитить фикс

## 📚 Дополнительные ресурсы

- [PRD документ](./prd.md) - продуктовые требования
- [BACKLOG](./BACKLOG.md) - текущие задачи и планы
- [Jest Documentation](https://jestjs.io/)
- [React Testing Library](https://testing-library.com/react)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)

---

**Помни**: Качество кода важнее скорости разработки. Лучше потратить время на тесты сейчас, чем тушить пожары в продакшене потом.
